- if user_signed_in?
    = link_to new_product_path,data:{turbolinks: false} do
        .exhibit
            .exhibit__text 出品
            = fa_icon("camera")
- else
    = link_to new_user_session_path do
        .exhibit
            .exhibit__text 出品
            = fa_icon("camera")

